{
  "name": "mgl-blog",
  "version": "1.0.0",
  "description": "MGL的个人博客 - VitePress + 阿里云",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "node deploy.js",
    "setup-server": "node deploy.js setup"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
